// package semagrams.sprites

// import semagrams._
// import semagrams.acsets._

// /** An [[SpriteSource]] that just returns a fixed list of entities. */
// def GenericEntitySource() =
//   SpriteSource[Seq[(Entity, Sprite, ACSet)]]((entities, _) => entities)
